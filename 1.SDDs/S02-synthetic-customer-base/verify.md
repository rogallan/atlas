# Verify — S02 · Synthetic Customer Base

## Evidence checklist

- [ ] Test run showing schema and consistency tests passing.
- [ ] Reproducibility check: two generation runs with the same seed produce identical output
      (hash comparison recorded).
- [ ] Manual review confirming no entity resembles a real person or institution.
- [ ] `manifest.json` (or equivalent) documenting the seed and generation parameters used.

## Sign-off

- [ ] Reviewed against `spec.md` — all requirements (R1–R5) satisfied.
- [ ] Reviewed against `plan.md` — entity model and generation strategy match what was implemented.
- [ ] No task in `tasks.md` is checked without corresponding evidence above.
